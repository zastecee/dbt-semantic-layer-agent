version: 2

models:
  - name: metricflow_time_spine
    description: "Daily time spine required by the dbt Semantic Layer / MetricFlow to resolve time-based metric queries."
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: "Calendar day."
        granularity: day
